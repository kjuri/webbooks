.review
  .col-sm-1.review__user
    = review.user.username
  .col-sm-2.review__rating
    = review.rating
  .col-sm-9.review__content
    = review.content
    - if review.user == current_user
      = link_to fa_icon('edit'), edit_book_review_path(review.book, review)
