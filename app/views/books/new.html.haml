.container
  .row
    .col-sm-12
      %h2 Create new book
      = simple_form_for @book do |f|
        .row
          .col-sm-6
            .form-group
              = f.input :title, class: 'form-control'
            .form-group
              = f.input :subtitle, class: 'form-control'
            .form-group
              = f.input :short_description, class: 'form-control'
            .form-group
              = f.input :genre_ids, collection: Genre.all.sort_by(&:name), selected: @book.genres, label_method: :name
            .form-group
              = f.input :cover, as: :file, class: 'form-control'
          .col-sm-6
            .form-group
              = f.input :description, as: :text, input_html: { class: 'form-control', rows: 12 }
        .row
          .col-sm-12
            = f.submit 'Create book', class: 'btn btn-success'
